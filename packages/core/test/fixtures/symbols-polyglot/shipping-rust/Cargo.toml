[package]
name = "shipping"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["full"] }
sqlx = { version = "0.7", features = ["runtime-tokio", "postgres"] }
opentelemetry = "0.21"

[dev-dependencies]
mockall = "0.12"
